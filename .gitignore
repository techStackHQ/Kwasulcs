# Credentials — never commit real config
config.php
.env
.env.*
!.env.example
!.env.production.example

# macOS metadata
.DS_Store

# Uploaded files
private_uploads/

# Composer dependencies (if added later)
vendor/
